// Utilizaremos la exportacion directa para la funcion declarada y la constante

export const circleArea = (radius: number): number => {
    return Math.PI * radius * radius
};

export const CIRCLE_NAME = 'Circle';